<%# app/views/games/show.html.erb %>
<div class="flex flex-col flex-3 gap-3 p-5 items-center">
  <h1 class="font-bold text-4xl"><%= @game.date.to_date %>
    vs
    <%= @game.opponent %></h1>
  <% if @game.not_started? %>
    <%= render "pregame", game: @game, rosters: @rosters %>
  <% elsif @game.in_progress? %>
    <%= render "in_progress", game: @game, rosters: @rosters %>
  <% end %>

  <%= link_to "Game List",
  season_path(@game.season_id),
  class:
    "border-2 border-blue-400 px-4 py-2 rounded-4xl text-blue-500 hover:bg-blue-400 hover:text-white" %>
  <% if @game.not_started? && current_user.can_edit?(@game.season.team) %>
    <%= link_to "Update Game Roster",
    game_game_rosters_path(@game),
    class:
      "border-2 border-blue-400 px-4 py-2 rounded-4xl text-blue-500 hover:bg-blue-400 hover:text-white" %>
    <%= button_to "Generate Fielding Lineup",
    generate_lineup_game_path(@game),
    method: :post,
    class:
      "border-2 border-green-400 px-4 py-2 rounded-4xl text-green-500 hover:bg-green-400 hover:text-white" %>
    <%= button_to "Start Game",
    start_game_path(@game),
    method: :patch,
    class: "btn-primary" %>
  <% end %>
</div>
